# ToutiaoVideoClient 项目开发规则

## 项目目标

实现一个仿今日头条的视频播放流 + 搜索模块 Android 客户端 Demo。

项目分为基础功能和进阶功能两部分。基础功能保证视频流、播放、搜索、历史、结果页、推荐词、图文混排和本地 Fake 分页可用；进阶功能用于完善性能、播放器能力、内容形态、搜索和技术文档。

## 技术栈

- Kotlin
- Jetpack Compose
- Navigation Compose
- Media3 ExoPlayer
- Room
- Coil
- MVVM
- 本地 Fake 数据

## 已实现基础功能

1. 视频流页面
2. 上下滑切换
3. 播放 / 暂停
4. 进度条 / seek
5. 搜索中间页
6. 搜索历史
7. 搜索结果页
8. 推荐词跳转
9. 图文卡基础混排
10. 本地 Fake 数据分页

## 进阶目标

后续阶段按用户指定顺序逐个实现，不要一次做多个阶段。

1. 优化起播速度：实现预加载或预渲染，并通过数据说明优化幅度。
2. 支持清晰度切换。
3. 支持横屏播放。
4. 完善视频流内的其他卡片，比如图片卡。
5. 完善推荐词生成与匹配策略。
6. 完善搜索结果匹配策略。
7. 可选：提供搜索结果页跨平台复用方案，比如 H5 页面或 Kotlin Multiplatform 方案说明。
8. 补充技术文档，说明 AI 参与推荐词生成的部分。

## 开发规则

1. 每次只完成用户指定的一个阶段，不要擅自扩展。
2. 每个阶段开始前先输出：
   - 本阶段目标
   - 会修改哪些文件
   - 涉及哪些架构点
   - 如何验证
3. 输出阶段说明后，再开始修改代码。
4. 每个阶段完成后输出：
   - 修改文件列表
   - 已实现功能
   - 验证方式
   - 下一阶段建议
5. 不要写长篇无关解释。
6. 不要接后端。
7. 不要做登录、评论发布、点赞提交等需要真实账号或真实服务端的功能。
8. 横屏播放、清晰度切换、预加载等进阶播放器能力，只有在用户明确指定对应阶段时才实现。
9. 不要大规模重构基础功能。
10. 优先保证项目能编译运行。
11. 代码优先简单清晰，便于学习架构。

## 验证要求

每个阶段完成后优先运行：

```powershell
.\gradlew.bat testDebugUnitTest
.\gradlew.bat assembleDebug
```

如果某个阶段只改文档，可以不运行 Gradle，但需要说明未运行的原因。

## 文档要求

涉及架构、播放器、推荐词、搜索策略、性能优化或 AI 辅助说明的阶段，需要同步更新：

- `README.md`：适合提交作业的项目说明。
- `docs/technical_design.md`：技术方案、数据结构、流程图和关键策略。
- `docs/runbook.md`：运行和排查说明。
- `docs/ai_usage.md`：AI 参与说明。

具体更新哪些文档，以当前阶段需求为准，不要为了文档而大范围改动无关内容。
